#include "../data/boolean.h"

/*
bool is_char_separator(char c, char separators[], int separators_amount);
*/



/*char[] is_first_word_is(int *offset, char line[], char next_word[], char separators[], int separators_amount){
    int i = 0;
    char word[MAX_WORD_SIZE];

    while (is_char_separator(line[i+*offset], separators, separators_amount)==FALSE) {
        if(next_word[i] != line[i+*offset])
            return FALSE;

        i++;
    }
    if()
    offset
    return TRUE;
}*/

/*
bool is_char_separator(char c, char separators[], int separators_amount){
    int i;
    for(i=0; i<separators_amount; i++){
        if(c==separators[i])
            return TRUE;
    }

    return FALSE;
}*/
